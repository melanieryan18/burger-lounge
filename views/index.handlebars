<h1>Eat a burger</h1>

<h2>Nom nom burger</h2>

<ul>
  {{#each burgers}}
  {{/each}}
</ul>

{{!-- <ul>
  {{#each burgers}}
    {{#if devoured}}
      {{>devoured=true}}
    {{/if}}
  {{/each}}
</ul> --}}

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Welcome 2 Burgerland</label>
    <input type="text" id="burger" name="name">
  </div>

  <button class="add-burger" data-id="{{id}}">ADD IT</button>

  <button class="del-burger" data-id="{{id}}">DEVOUR IT</button>
</form>
